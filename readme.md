# Illusion Plugin Architecture + Plugins by EEE

## Notes

- Make sure you use the right build configuration. Aside from "Debug" and "Release" there's also "PCDeploy" and "HMDeploy" which are configured for my personal system.